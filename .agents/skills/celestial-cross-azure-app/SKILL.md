---
name: celestial-cross-azure-app
description: Detalhes do ambiente do Azure, incluindo o nome do app para deploy (CelestialCrossFuncs1206FE).
---

# Azure App Environment

Você está trabalhando no projeto **Celestial Cross**.
Sempre que precisar executar comandos de deploy via `func azure functionapp publish` ou atualizar variáveis via Azure CLI (`az functionapp config appsettings set`), ou sempre que o usuário pedir comandos para Azure:

- O nome do aplicativo (App Name) do Azure Function é: **CelestialCrossFuncs1206FE**

Exemplo de uso em deploy:
```powershell
func azure functionapp publish CelestialCrossFuncs1206FE
```
